import { CurriculumModuleFilterPipe } from './curriculum-module-filter.pipe';

describe('CurriculumModuleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurriculumModuleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
