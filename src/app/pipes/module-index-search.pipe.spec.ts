import { ModuleIndexSearchPipe } from './module-index-search.pipe';

describe('ModuleIndexSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleIndexSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
