import { CurrModuleFilterPipe } from './curr-module-filter.pipe';

describe('CurrModuleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrModuleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
