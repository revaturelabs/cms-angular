import { ContentOrderPipe } from './content-order.pipe';

describe('ContentOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
