import { CurriculumFilterPipe } from './curriculum-filter.pipe';

describe('CurriculumFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurriculumFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
